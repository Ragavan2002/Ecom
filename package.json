{
  "name": "ecom",
  "version": "1.0.0",
  "description": "Eccomerce",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "cd store && yarn install",
    "dev": "cd store && yarn dev",
    "build": "cd store && yarn build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "vue-toast-notification": "^3.1.3"
  }
}
